package assets {
    
    import assets.Assets;
    import assets.MovieClipWrapper;
    
    public class emoDeathClass extends MovieClipWrapper {
        
        public function emoDeathClass() {
            image = new assets.Assets.emoDeathClass();
            this.addChild(image);
        }

    }
}
